{
  "name": "national-controls-consulting-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "parcel project-root/index.html --port 3000",
    "build": "parcel build project-root/index.html --dist-dir public --public-url ./",
    "build:full-site": "parcel build project-root/index.html --dist-dir public --public-url ./",
    "build:coming-soon": "parcel build project-root/index.html --dist-dir public --public-url ./ && cp project-root/coming-soon.html public/index.html",
    "preview": "npm run build && python3 -m http.server --directory public 8000"
  },
  "devDependencies": {
    "parcel": "^2.9.3"
  }
}
