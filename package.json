{
  "name": "national-controls-consulting-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "parcel project-root/index.html --port 3000",
    "build": "parcel build project-root/index.html --dist-dir public --public-url ./"
  },
  "devDependencies": {
    "parcel": "^2.9.3"
  }
}
