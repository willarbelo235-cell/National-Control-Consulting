# Project root

This folder contains a minimal static site:

- `index.html`
- `style.css`
- `script.js`
- `images/logo.png` (generated by `create_logo.py`)

To generate the placeholder image run:

```bash
python3 create_logo.py
```
